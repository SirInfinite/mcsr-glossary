// This file is public by design. Use only a Supabase publishable key (or legacy anon key).
// Never place an sb_secret_ key or legacy service_role JWT in frontend code.
window.MCSR_CONFIG = Object.freeze({
    supabaseUrl: "",
    supabasePublishableKey: ""
});
